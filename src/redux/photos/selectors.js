export const selectUserProfilePhoto = (state) => state.photos.userProfilePhoto;
export const selectAdvertPhotos = (state) => state.photos.advertPhotos;
export const selectPhotoFiles = (state) => state.photos.photoFiles;
export const selectIsLoading = (state) => state.photos.isLoading;
export const selectError = (state) => state.photos.error;